# MongoDB connection - only MONGODB_URI is read.
# Demo writes into the `csx_demo` database (override with CSX_DEMO_DB).
MONGODB_URI=mongodb://dbuser:donotpass@localhost:27017/?authSource=admin&directConnection=true
# CSX_DEMO_DB=csx_demo

# API server
API_HOST=0.0.0.0
API_PORT=8088

# Simulation cadence
SIM_INTERVAL_SECONDS=5.0
SIM_STEP_KM=18.0
